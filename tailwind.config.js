const defaultTheme = require("tailwindcss/defaultTheme");

module.exports = {
  darkMode: "class",
  theme: {
    extend: {
      // Fonts - Add extra fonts here
      fontFamily: {
        fonttName: ["fontName", "sans-serif"],
      },

      // Colors - Configure your project colors here
    },
  },
  plugins: [],
};
